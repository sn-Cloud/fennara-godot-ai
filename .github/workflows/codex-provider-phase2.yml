name: Codex Provider Phase 2

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: codex-provider-phase2
  cancel-in-progress: false

jobs:
  approval-routing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout development branch
        uses: actions/checkout@v4
        with:
          ref: feature/codex-embedded-provider
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Apply phase-two changes
        run: python .github/scripts/apply_codex_provider_phase2.py

      - name: Format Rust
        working-directory: local
        run: cargo fmt --all

      - name: Test daemon
        working-directory: local
        run: cargo test --locked -p fennara-daemon

      - name: Remove one-time migration files
        shell: bash
        run: |
          rm -f .github/scripts/apply_codex_provider_phase2.py
          rm -f .github/workflows/codex-provider-phase2.yml
          rm -f .github/codex-provider-phase2.trigger

      - name: Commit verified changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(codex): route native approvals through Fennara"
          git push origin HEAD:feature/codex-embedded-provider
