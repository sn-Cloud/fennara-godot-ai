name: Codex Provider Phase 8

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: codex-provider-phase8
  cancel-in-progress: false

jobs:
  managed-runtime:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout development branch
        uses: actions/checkout@v4
        with:
          ref: feature/codex-embedded-provider
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Apply phase-eight changes
        run: python .github/scripts/apply_codex_provider_phase8.py

      - name: Format Rust
        working-directory: local
        run: cargo fmt --all

      - name: Test managed runtime and daemon
        working-directory: local
        run: cargo test --locked -p fennara-daemon

      - name: Remove one-time migration files
        shell: bash
        run: |
          rm -f .github/scripts/apply_codex_provider_phase8.py
          rm -f .github/workflows/codex-provider-phase8.yml
          rm -f .github/codex-provider-phase8.trigger
          rm -f .github/codex-phase7-platform-validation.trigger

      - name: Commit verified changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(codex): install and repair managed Windows runtime"
          git push origin HEAD:feature/codex-embedded-provider
